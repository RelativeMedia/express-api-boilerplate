'use strict';

// config/log.js

// LOGGING CONFIGURATION
// =============================================================================
var logConfig = {
    level: 'debug',
    handleExceptions: true,
    colorize: true,
    prettyPrint: true
};

module.exports = logConfig;