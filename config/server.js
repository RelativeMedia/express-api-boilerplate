'use strict';

// config/server.js

// SERVER CONFIGURATION
// =============================================================================
var serverConfig = {
  prefix: '/api',
};

module.exports = serverConfig;